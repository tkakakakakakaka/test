<form method="post" action="{{ path('app_traitement_delete', {'cid': consultation.id, 'tid': traitement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traitement.id) }}">
    <button class="btn">Delete</button>
</form>
